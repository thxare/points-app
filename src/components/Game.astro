---
import styles from "./Game.module.css";
import Button from "../ui/Button.astro";
const { id, game } = Astro.params;
---

<div class={styles.container}>
  <h1>{id}</h1>
  <Button title="Atrás →" path={`/games/${game}`} target={false} />
</div>
